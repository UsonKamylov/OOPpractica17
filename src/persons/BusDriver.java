package persons;

import transport.Bus;
import transport.Car;

public class BusDriver extends Person {
    public BusDriver(String name, int age, double weight, String specialist) {
        super(name, age, weight, specialist);
    }
}
